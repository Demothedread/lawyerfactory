# Bartleby Chatbot - Additional Python Dependencies
# Add these to your requirements.txt

# OpenAI API client
openai>=1.0.0

# Anthropic Claude API client
anthropic>=0.18.0

# Ollama doesn't require a separate package - uses HTTP requests

# Flask is already in requirements.txt

# For GitHub Copilot integration (if using)
# github>=2.1.0
